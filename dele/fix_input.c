#include "minishell.h"

char *fix_input(char *str)
{
	int	i;
	char c;
	t_operator op;

	while (str[i])
	{
		op = is_opperator(str[i]);
		if (op != -1 || str[i-1] != )
		{

		}
		if (op == )
		if ()
	}
}